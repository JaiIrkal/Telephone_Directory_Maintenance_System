
    // printf(" last line =%s",str);
    // printf("Size of last line is = %d\n",strlen(str));

    // int i =0;
    // char substring[4];

    // char *p = strchr( str, '\n' );

    //     if ( !p ) fscanf( stdin, "%*[^\n]\n" );
    //     else *p = '\0';
    //     puts(str);